-------------------
Es werden Java Version 11 und JavaFX version 11.0.1 gebraucht um das Programm ausführen zu können.
Um die .jar zu starten muss folgendes in das Terminal eingegeben werden (In dem Ordrner wo sich die .jar Datei befindet).

java --module-path $PATH_TO_JAVAFXLIB --add-modules=javafx.controls,javafx.fxml -jar ProjectPineappleG.jar

Anstatt $PATH_TO_JAVAFXLIB muss der entsprechende Pfad zu dem lib Ordner vom javafx-sdk-11.0.1 angegeben werden.
-------------------

-Netzwerk wurde fertiggestellt und getestet.
-Netzwerk in Programm eingebunden und mit den jeweiligen Schnittstellen (Model und View) connected.


-Klick  auf Host-Game Button setzt einen Server auf und registriert den Host-Client im Netzwerk.
-„externe“ Clients können sich auf dem Server erfolgreich connecten.
-Ab 2 Clients ist für den Hoster der Start-Game Button enabled und er kann das Spiel starten.
-Draw-Tile funktioniert.